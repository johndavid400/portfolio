<%= render 'refinery/admin/search_header', :url => refinery.portfolio_images_admin_portfolio_images_path %>
<div class='pagination_container'>
  <% if @portfolio_images.any? %>
    <%= render 'portfolio_images' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
