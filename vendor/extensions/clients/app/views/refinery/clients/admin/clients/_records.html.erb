<%= render 'refinery/admin/search_header', :url => refinery.clients_admin_clients_path %>
<div class='pagination_container'>
  <% if @clients.any? %>
    <%= render 'clients' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
